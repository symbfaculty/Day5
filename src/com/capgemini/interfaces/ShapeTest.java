package com.capgemini.interfaces;

import java.util.Random;

import com.capgemini.domain.Car;
import com.capgemini.domain.Truck;

public class ShapeTest {

	public static void main(String[] args) {
		
		/*System.out.println(" "+Shape.PI);
		Shape shapes[] = new Shape[10];
		Random random = new Random();
		for (int i = 0; i < shapes.length; i++) {
			int value = random.nextInt(5); // 0 circle 1 rectangle 2 triangle 3 car 4 truck
			switch (value) {
			case 0:
				shapes[i] = new Circle();
				break;
			case 1:
				shapes[i] = new Rectangle();
				break;
			case 2:
				shapes[i] = new Triangle();
				break;
			case 3:
				shapes[i] = new Car();
				break;	
			case 4:
				shapes[i] = new Truck();
				break;
			}
			
		}	
		for(Shape shape:shapes) {
			shape.draw();
		}*/
		
	/*double number=Math.random()*10000;
	System.out.println(number);
	int x=(int)number;
	System.out.println(" "+x);*/
		/*
		 * Shape shape= new Circle(); shape.draw(); shape=new Rectangle(); shape.draw();
		 * shape=new Triangle(); shape.draw();
		 */
	}

}
